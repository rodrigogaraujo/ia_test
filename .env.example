# URL pública da API (usada no build do Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3456

# Senha do Redis (OBRIGATÓRIA — usada no docker-compose)
# Gere uma senha forte: openssl rand -base64 32
REDIS_PASSWORD=
